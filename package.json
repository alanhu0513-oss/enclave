{
  "name": "enclave",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install",
    "start": "node backend/src/index.js"
  }
}
